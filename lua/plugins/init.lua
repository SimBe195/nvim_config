return {
    unpack(require 'plugins.core'),
    unpack(require 'plugins.completion'),
    unpack(require 'plugins.editing'),
    unpack(require 'plugins.lsp'),
    unpack(require 'plugins.navigation'),
    unpack(require 'plugins.tools'),
    unpack(require 'plugins.ui'),
}
